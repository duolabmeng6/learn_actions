name: 学习

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  s1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - name: 运行一句脚本
        run: echo 你好
      - name: 运行多行脚本
        run: |
          echo 大家好
          echo 我叫哆啦b梦
