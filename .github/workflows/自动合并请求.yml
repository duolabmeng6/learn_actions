name: 自动合并请求

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, reopened, synchronize]
  pull_request_target:
    types: [opened, reopened, synchronize]

  workflow_dispatch:

permissions: write-all

jobs:
  update_release_draft:
    name : 自动合并请求
    runs-on: ubuntu-latest
    steps:
    - name: Run merge script
      uses: klay-ke/auto-merge-pr@main
      id: merge-pr
      with:
        #send-to-dingtalk-group: true
        #dingtalk-access-token: ${{ secrets.DINGTALK_ACCESS_TOKEN }}
        #dingtalk-secret: ${{ secrets.DINGTALK_SECRET }}
        maintainer-team-name: duolabemng6
        gh-token: ${{ secrets.GITHUB_TOKEN }}
        #ci-command: 'bash ./build.sh'